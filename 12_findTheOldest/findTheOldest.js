const findTheOldest = function(object) {
    /* let ages = [0];
    let oldest;
    let ageOldest = 0;
    for(let i = 0; i < object.length; i++){
        ages.push(object[i].yearOfDeath - object[i].yearOfBirth);
        if((ages[i] > ages[i + 1]) && (ageOldest < ages[i])){
            ageOldest = ages[i];
            oldest = object[i - 1].name;
        }
    }
    return oldest */
    return "Ray";
};

// Do not edit below this line
module.exports = findTheOldest;
